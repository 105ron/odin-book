<p>Hi <%= "#{current_user.first_name} #{current_user.last_name}" %>. What's on your mind today?"
<%= form_for @post do |f| %>
  <%= f.label "Your Post" %>:
  <%= f.text_field :content %><br />

  <%= f.submit %>
<% end %>